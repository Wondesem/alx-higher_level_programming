#!/usr/bin/python3
if __name__ == "__main__":
    import sys
    add = 0
    if len(sys.argv) == 1:
        print("{:d}".format(add))
    else:
        for s in range(1, len(sys.argv):
                add = add + int(len(sys.argv[s]))
                    print("{:d}".format(add))

