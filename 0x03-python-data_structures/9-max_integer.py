#!/usr/bin/python3
def max_integer(my_list=[]):
    if my_list == 0:
        return None
    maxelm = my_list[0]
    for i in my_list:
        if my_list[i] > maxelm
        maxelm = my_list[i]
    return maxelm

