#!/usr/bin/python3
def max_integer(my_list=[]):
    if my_list:
        maxelm = my_list[0]
        for i in range(len(my_list)):
            if my_list[i] > maxelm
            maxelm = my_list[i]
            return (maxelm)
        return (None)
